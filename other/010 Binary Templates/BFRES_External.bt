// ==== Structures =====================================================================================================

typedef struct // ExternalFile
{
	Offset dataOffset;
	uint length;
	FSeek(dataOffset.target);
	byte data[length];
} ExternalFile <bgcolor=0xE9E9E9>;
